# Crypto Quest Admin

1. Clone the project:

```shell
git clone https://github.com/anth226/cryptoquest-admin.git
```

2. Install dependencies:

```shell
cd cryptoquest-admin
npm install
```

3. Run locally:

```shell
npm start
```
